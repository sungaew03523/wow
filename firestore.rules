
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем чтение и запись во все коллекции
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
